# ----------------------------------------
# Node.js and NPM Specific Ignore Rules
# ----------------------------------------

# Dependency directories
# Ignores the main node_modules in the root (if it existed)
# and also covers the ones in 'client' and 'server' directories
node_modules/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Runtime data
*.pid
*.seed
*.log

# Generated files
dist/
build/
coverage/

# Environment and configuration files
.env
.env.*
.DS_Store
.vscode/

# ----------------------------------------
# Explicit rules for the 'client' directory
# ----------------------------------------
client/node_modules/
client/dist/
client/build/
# You might want to ignore client-specific build artifacts here,
# like static assets generated by a frontend build tool (e.g., 'client/out/')

# ----------------------------------------
# Explicit rules for the 'server' directory
# ----------------------------------------
server/node_modules/
server/dist/
server/build/
# Any compiled or generated server artifacts go here
# e.g., if you compile TypeScript to JS in a specific folder